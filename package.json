{
  "name": "ghost-storage-cloudflare-images",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --outfile=index.js --platform=node --format=cjs --target=node16 --packages=external --footer:js='module.exports = module.exports.default;'",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudflare-images-client": "^1.1.5",
    "ghost-storage-base": "^1.1.1"
  },
    "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/ghost-storage-base": "^0.0.1",
    "esbuild": "^0.20.2",
    "prettier": "^3.2.5",
    "tslib": "^2.6.2",
    "typescript": "^5.4.3"
  }
}
