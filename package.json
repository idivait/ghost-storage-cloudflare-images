{
  "name": "ghost-storage-cloudflare-images",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev:esbuild & npm run logs",
    "dev:esbuild": "esbuild ./src/index.ts --watch --bundle --outfile=index.js --platform=node --format=cjs --target=node16 --packages=external --footer:js='module.exports = module.exports.default;'",
    "build": "esbuild ./src/index.ts --bundle --outfile=index.js --platform=node --format=cjs --target=node16 --packages=external --footer:js='module.exports = module.exports.default;'",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs": "docker compose logs ghost -f",
    "prelogs": "docker compose up -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tryghost/logging": "^2.4.23",
    "cloudflare": "^4.5.0",
    "esbuild": "^0.25.9",
    "ghost-storage-base": "^1.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/ghost-storage-base": "^0.0.1",
    "prettier": "^3.2.5",
    "tslib": "^2.6.2",
    "typescript": "^5.4.3"
  }
}
